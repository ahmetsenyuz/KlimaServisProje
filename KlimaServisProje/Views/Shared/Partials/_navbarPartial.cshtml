@inject SignInManager<ApplicationUser> SignInManager
<!-- =============== nav =============== -->
<nav id="mainNav" class="navbar navbar-default navbar-fixed-top">
    <div class="container">
        <div class="container-fluid">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a>
                    <h1 style="color:white; font-family:Verdana">Klima Servis</h1>
                </a>
            </div>

            <!-- Collect the nav links, forms, and other content for toggling -->

            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav navbar-right">
                    <li>
                        <a class="page-scroll" asp-controller="Home" asp-action="Index">Anasayfa</a>
                    </li>
                    <li>
                        <a class="page-scroll" href="#how-it-works">Hakkımızda</a>
                    </li>
                    @if (SignInManager.IsSignedIn(Context.User))
                    {
                        <li class="page-scroll"><a asp-controller="TroubleRegister" asp-action="CreateReport">Kayıt Oluştur</a></li>

                        <li class="page-scroll"><a asp-controller="TroubleRegister" asp-action="Reports">Raporlarım</a></li>

                        <li class="page-scroll"><a asp-controller="account" asp-action="profile">Profil</a></li>
                        <li>
                            <a asp-controller="account" asp-action="logout" class="page-scroll">Çıkış Yap</a>
                        </li>
                    }
                    else
                    {
                        <li>
                            <a asp-controller="account" asp-action="login" class="page-scroll">Giriş Yap</a>
                        </li>
                    }

                </ul>
            </div>
            <!-- =============== navbar-collapse =============== -->

        </div>
    </div>
    <!-- =============== container-fluid =============== -->
</nav>